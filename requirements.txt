# Dependências do projeto Fuja do Mico
# Instalar com: pip install -r requirements.txt

# ── API da Anthropic (Claude) ──
anthropic>=0.40.0

# ── Google APIs (Gmail + YouTube) ──
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0

# ── RSS ──
feedparser>=6.0.10

# ── HTTP requests ──
requests>=2.31.0

# ── Notificações ──
# Telegram: sem dependência externa — usa urllib da stdlib

# ── Banco de dados ──
supabase>=2.0.0

# ── Utilitários ──
python-dateutil>=2.8.2
isodate>=0.6.1

# ── Transcrições YouTube (sem OAuth) ──
youtube-transcript-api>=0.6.2
